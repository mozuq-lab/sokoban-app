name: sokoban_app
description: A minimal Sokoban puzzle game.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.0.0
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0
